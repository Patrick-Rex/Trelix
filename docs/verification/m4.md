# M4 编辑工作区进度记录

M4 的前端实现已接入，主要管理与编辑流程已在 Aspire 启动的真实页面和 SQLite 上验证。工程检查及生产构建预览通过；AC-07 尚有人工交互和受控故障场景未覆盖，因此 M4 保持进行中。

## 实现范围

- [App.vue](../../src/frontend/src/App.vue)、[LoginPage.vue](../../src/frontend/src/features/authentication/components/LoginPage.vue) 与认证 API 接入管理员会话、防伪造令牌、登录、退出和会话失效处理。
- [WorkspacePage.vue](../../src/frontend/src/pages/WorkspacePage.vue)、[WorkspaceResourceTree.vue](../../src/frontend/src/features/workspace/components/WorkspaceResourceTree.vue) 和 [useWorkspace.ts](../../src/frontend/src/features/workspace/composables/useWorkspace.ts) 接入项目、环境、文件资源树，资源创建、重命名、删除、选择和未保存提示。
- [EditorPanel.vue](../../src/frontend/src/features/workspace/components/EditorPanel.vue)、[configDocument.ts](../../src/frontend/src/features/workspace/utils/configDocument.ts)、[MonacoCodeEditor.vue](../../src/frontend/src/features/workspace/components/MonacoCodeEditor.vue) 与 [JsonTreeEditor.vue](../../src/frontend/src/features/workspace/components/JsonTreeEditor.vue) 提供共用文档状态下的 JSON、YAML 和 Tree 编辑；防抖校验保留当前无效文本，保存和发布前同步校验，Monaco 使用 Vite ESM workers。
- [HistoryDialog.vue](../../src/frontend/src/features/workspace/components/HistoryDialog.vue) 和工作区状态管理接入草稿保存、发布、历史预览、回滚、请求过期隔离及并发冲突提示。
- [ApplicationTokensPage.vue](../../src/frontend/src/features/application-tokens/pages/ApplicationTokensPage.vue) 与令牌 API 接入授权范围选择、有效期、创建、一次性显示、复制、轮换和撤销。
- [ConnectionConfigButton.vue](../../src/frontend/src/features/workspace/components/ConnectionConfigButton.vue) 按当前文件生成并一键复制 `Trelix` 连接节，提供服务地址调整、占位令牌、未发布提示与剪贴板失败或超时后的手动复制预览；字段约定与 M5 SDK 共用 [架构定义](../architecture.md#sdk-与宿主边界)。
- [HTTP 管理客户端](../../src/frontend/src/shared/api/http.ts) 统一处理同源 Cookie、防伪造令牌、管理 API 错误和会话失效事件。

## 已执行验证

- 在 `src/frontend` 执行 `npm run type-check`：退出码 0。
- 在 `src/frontend` 执行 `npm run build`：退出码 0。
- 在项目根目录执行 `dotnet build .\Trelix.slnx --nologo -v:q -clp:ErrorsOnly`：退出码 0，错误数为 0。
- 在 `src/frontend` 执行 `npm exec -- oxlint src` 和 `npm exec -- eslint src`：退出码均为 0。
- 按 [本地开发](../local-development.md#统一启动与联调) 使用 Aspire HTTPS Profile 联合启动 Server 与前端，仪表板确认两个资源运行。使用用户提供的现有管理员账号完成登录；凭据未写入项目文件。
- 通过 Aspire 前端资源临时运行 Vite 生产预览，确认页面加载打包后的 `assets` 脚本、Monaco 可编辑，Tree 修改同步至 JSON，保存产生新草稿修订且当前发布版本不变；该页面控制台没有 error 或 warn。随后恢复原开发脚本并由 Aspire 启动。此项不代表 M6 的 Server 静态资源托管或容器交付已完成。
- 本次未运行自动化测试；前端当前没有测试脚本。

### 登录后的实际操作

使用单独的合成验收项目、环境和文件执行以下流程，未使用真实业务配置：

- 从空资源树创建项目、环境及两个文件，资源位置、草稿修订和当前发布可见。
- JSON、YAML、Tree 转换保持对象、数组、字符串、数字、布尔和 null；YAML 注释与锚点名称被丢弃，别名展开后的值保留。
- 无效 JSON、YAML 和 Tree 数字输入后立即保存、发布或切换表示均被阻止，当前输入保留。有效文本输入后立即发布，先保存新草稿再发布。
- Tree 数值修改后发布，历史正文反映新值；回滚旧历史生成更高发布版本并标注来源，草稿修订不变；单独保存不改变当前发布版本。
- 两个页面从同一草稿修订编辑，后保存的旧页面收到冲突提示，保留当前文本并提供重新载入入口。
- 未保存文件切换时取消确认保留当前输入，确认后载入目标文件；Monaco 撤销恢复原正文，侧栏折叠后编辑区尺寸随容器变化。
- 应用令牌指定验收环境和未来有效期创建，签发及轮换均出现一次性凭证窗口；轮换后原令牌已撤销，随后撤销后继，列表确认有效令牌数为零。未复制或保存令牌原文，验收资源及撤销记录保留。
- 另一页面退出同一会话后，编辑页保存收到 401；重新登录后原未保存正文仍在。
- 缩小浏览器后实际页面宽度约为 785 CSS 像素，未发现横向溢出；浏览器工具未达到请求的手机宽度，因此不将此项记为手机窄屏通过。

### 发现并修复的问题

- Tree 曾对 Vue 代理直接执行 `structuredClone`，导致修改未同步。改为先 `toRaw`，重新编辑、发布并核对历史正文通过。
- Tree 的不完整数字和未完成属性名输入纳入统一校验，避免保存上一份有效数据；数组项可删除，对象属性写入支持特殊键名。
- 未保存、回滚、退出及撤销确认统一使用 Element Plus；资源提交失败保留表单输入，防止重复提交。
- 资源列表按分页完整读取，资源读取响应检查当前选择及取消信号；组件卸载取消请求并释放编辑器，具体释放行为仍须专项观察。
- YAML 解析异常统一转换为不包含正文的提示，401/403 使用中文状态说明。
- 新建和重命名曾共用当前资源的初始值，导致新建默认重名；现仅重命名预填，新建项目、环境与文件留空。
- 主导航展开与收起入口采用侧栏底部独立工具区与图标按钮，页面滚动时保持可见；窄屏收起后保留图标栏与底部展开入口。底部固定开关模式参考 [Ant Design 侧栏实现](https://github.com/ant-design/ant-design/blob/master/components/layout/style/sider.ts)，沿用现有 Element Plus 图标与原生按钮实现。
- 资源操作按层级归位：项目新增、重命名和删除位于项目标题旁，环境对应操作位于环境标题旁；统一使用带可访问名称和悬停提示的图标按钮，加载期间禁用。

### 2026-09-23 交互改进验证

- 前端 `npm run build` 通过，包含 `vue-tsc` 类型检查；本次修改的 Vue 文件通过 Oxlint 与 ESLint 检查，未改动依赖或接入新测试框架。
- 临时 Vite 页面直接挂载实际 `AppShell` 和 `WorkspacePage`，资源读取使用独立的合成数据；未连接真实 Server 或数据库，未执行资源写入。以下结果只覆盖组件交互，不替代上文的真实 API 验收。
- 选中项目、环境与文件后，分别打开新建表单，名称为空；环境重命名预填业务标识和显示名，关闭后再次新建环境不残留输入。
- 在实际 1280 与 390 CSS 像素视口下检查侧栏，按钮位于侧栏内；手机宽度收起后保留 64 像素图标栏，按 Enter 可收起并再次展开，页面未出现横向溢出。
- 连接配置使用业务标识；包含引号与中文的文件名经预览 JSON 解析后保持原值。修改服务地址后预览更新且旧成功提示清除；非 HTTP/HTTPS 地址及带查询参数的地址禁止复制。未发布文件显示提示，切换到已发布文件后提示消失。
- 浏览器剪贴板写入返回成功并显示成功提示；工具的虚拟剪贴板无法读回原生写入内容，因此未把实际粘贴内容核对记为通过。剪贴板拒绝和超时后的手动复制路径尚未进行受控故障验证。
- 本轮未运行 .NET 构建或自动化测试；未验证 SDK 消费生成片段，相关接入仍属 M5。
- 后续资源操作分组调整再次通过前端构建（含类型检查）、Oxlint 与 ESLint。临时页面以合成数据挂载实际 `WorkspaceResourceTree`，在 224 像素资源栏中检查空项目与空环境布局，并逐个点击新增、重命名、删除按钮，确认六个事件分别携带正确的项目或环境类型；未调用真实删除 API，验证后停止临时服务。
- 项目标题对齐与侧栏底部按钮调整后，再次通过前端构建（含类型检查）、Oxlint 与 ESLint。合成页面挂载实际 `AppShell` 与资源树：项目标题和操作区垂直中心差为 0 像素，项目与环境操作区右边缘差为 0 像素；页面向下滚动后按钮底部仍距视口底边 10 像素。1280 与 390 CSS 像素宽度下展开、收起和 Enter 操作通过，手机宽度无横向溢出；未连接真实 API，临时服务在验证后停止。
- 提交前修复 Tree 数组按下标复用组件导致的输入与校验残留：数组项改用稳定身份，校验标识随节点实例保留。临时页面挂载实际 `JsonTreeEditor`，验证不同值及相同值数组中删除无效数字项后恢复有效状态；删除前项时，后项及其嵌套属性的无效输入仍保留，修正后校验通过；删除带属性名错误的子树后校验清除，空数组可继续新增。页面控制台无 error 或 warn，前端构建（含类型检查）、全 `src` Oxlint 与 ESLint 通过。此项仅验证组件数据和校验事件，未调用真实保存 API；临时服务在验证后停止。

## 尚未完成的验收

- 真实中文输入法组合输入，以及复杂光标、选区、长文档滚动行为；粘贴中文不视为输入法验收。
- Monaco worker 的实际加载路径、切换或卸载后的资源释放专项观察。
- 受控延迟下的旧请求隔离、403 与断网等故障注入场景；现有真实页面已覆盖 401、并发冲突和格式校验失败。
- 真实认证入口下的手机布局及完整资源重命名、删除流程；本轮合成数据验证已覆盖侧栏手机宽度和环境重命名表单初始值。
- 复制结果的实际粘贴核对，以及剪贴板拒绝、超时后的回退路径；生成片段配合外部令牌接入示例已在 [M5 验收](m5.md) 验证，此结果不替代原生剪贴板核对。

以上为验证覆盖的边界，不表示已发现这些场景失败。AC-07 尚未完整通过，未提前标记 M4 完成。
