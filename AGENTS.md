# Trelix 开发指引

## 协作与入口

- 用中文沟通，先核实代码和事实；无法解决的问题交给用户，不猜测业务规则。已确认事项直接执行，新产品规则、基础设施或技术路线先讨论，不阻塞独立工作。
- 工作范围限于包含 `Trelix.slnx` 的目录；即使 Git 顶层在父目录，也不越界搜索、修改或提交，保留用户已有改动。
- 开始先读 [技术基线](docs/development-baseline.md)，按 [文档索引](docs/README.md#按任务读取) 补充当前任务所需的产品、架构与验收内容。后端任务先读 [后端公共指引](src/backend/AGENTS.md)；前端与编排任务分别读 [前端](src/frontend/AGENTS.md)、[AppHost](src/Trelix.AppHost/AGENTS.md)。选型以技术基线为准，工程事实核对源码、依赖清单和实际验证结果，不把“已确认”写成“已实现”。
- 按 [技能索引](.agents/README.md) 读取相关技能及必要参考。项目事实和已确认基线优先于上游默认值；技能安装不等于采用其依赖或方案，已有授权不重复确认。不加载上游技能创作/评估/市场维护指引，也不复制其 `AGENTS.md`。

## 实现与文档

- 手写类、记录、接口及方法（含构造函数、非公开方法和测试方法）必须有标准文档注释：C# 使用 XML 文档注释，JavaScript/TypeScript 使用 JSDoc；说明职责，按签名补充参数、泛型参数、返回值及必要的异常或副作用，避免仅复述名称。重写或接口实现可在语义一致时使用 `<inheritdoc />`；生成代码由生成器维护，不手工补注释。
- .NET 使用现代语言与运行时能力，保持基线兼容；性能优化以证据为依据，优先减少 I/O、重复查询和分配，避免无收益的抽象。
- 依赖服务于当前任务，沿用 npm 和现有锁文件；不自动切换包管理器、升级主版本或启用预览功能。
- 新增和修改文本文件遵循 [.editorconfig](.editorconfig) 与 [.gitattributes](.gitattributes)；写入后检查编码、BOM、行尾及文件末尾换行，不得产生混合行尾。格式规则见 [技术基线](docs/development-baseline.md)，不批量重写本次任务无关文件。
- 日志、错误响应和遥测不得记录原始配置正文、令牌或连接密钥。
- 修改或新增文档时不得写入本机路径、内网或本地服务地址、真实端口、密钥、令牌、密码、连接字符串及其他机密；示例统一使用不可用的占位符，必要的环境信息只描述获取方式和配置项名称。
- 非必要不新增或扩充 agent 指引内容，避免重复记录已有规则和无关细节。
- 每次变更同步更新所有受影响的 README、模块指引和技术基线，核对本目录、上级入口及引用文档的路径、命令与功能状态。技能新增、修改、重命名或删除，必须同轮更新各文档。
- 项目文档只记录已确认的方案、规则、里程碑和可核实的工程事实；讨论过程、候选方案和未决事项留在对话中，不写入项目文档。
- 根指引仅保留通用约束；各主题的主文档与更新关系见 [文档索引](docs/README.md)，后端共用规则放 `src/backend/AGENTS.md`，模块细节放模块 `AGENTS.md`，技能用法放技能索引。优先维护原条目和链接，避免重复追加；不改无关文档。

## 提交信息

- 提交信息遵循 [Conventional Commits 1.0.0](https://www.conventionalcommits.org/en/v1.0.0/)；标题格式为 `<type>: <简短描述>`，仅在确实有助于区分时使用可选 scope：`<type>(<scope>): <简短描述>`。
- `type` 只能使用 `feat`、`fix`、`docs`、`style`、`refactor`、`test`、`build`、`chore`、`perf` 或 `ci`；按改动的主要行为选择类型，而不按新增行数或注释数量选择。
- 标题单行且不超过 50 个可见字符，优先表达已交付的结果或目的；不使用“修改一下”“更新代码”等空泛措辞，不追加句号、表情、编号或 AI 生成说明。
- 无必要不写正文；需要补充背景、风险或迁移注意事项时，正文使用中文、最多三行且每行不超过 72 个字符。
- 破坏性变更在标题的类型或 scope 后加 `!`，并在正文以 `BREAKING CHANGE:` 说明迁移要求，例如 `feat(api)!: 调整应用配置读取契约`。

## 验证与交付

- **编译只读取 error 和退出码**：`dotnet build .\Trelix.slnx --nologo -v:q -clp:ErrorsOnly`，可缩小到相关 `.csproj`。子构建有额外输出时存临时日志，仅提取 error，不展开 warning 或修复无关问题。
- 前端检查遵循前端指引；测试可读失败详情和通过/失败数量，不把零测试视为通过。测试工具链以实际项目、依赖和脚本为准，接入后同步更新相关操作说明。
- 执行能验证行为的最小检查；纯文档/技能变更检查内容、链接及来源，无需构建业务项目。文档一致性是交付完成条件。
- 交付说明改动、实际验证与未解决项；按 [质量与验收](docs/quality.md) 核对相关场景和文档。环境或依赖阻塞如实交给用户，不编造成功结果，不提前标记里程碑完成。
